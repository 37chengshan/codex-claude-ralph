[example] This is the public progress log Ralph appends to while it runs.
[example] Replace `prompt.md` or import your own `prd.json`, then run `./ralph.sh init`.
[example] Runtime state, lock files, and run artifacts stay under `.codex-ralph/` and are not meant for publishing.
